/**
 * Shared in-memory transaction status store for the entire app.
 * Import and use this object in all routes that need to read or update transaction status.
 */
const transactionStatusStore = {};

export default transactionStatusStore;